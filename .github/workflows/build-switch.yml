name: Build Switch NRO

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: devkitpro/devkita64
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          apt-get update
          apt-get install -y --no-install-recommends cmake ninja-build python3

      - name: Build
        run: |
          export DEVKITPRO=/opt/devkitpro
          export DEVKITA64=/opt/devkitpro/devkitA64
          bash scripts/build_switch.sh

      - name: Rename NRO
        run: cp build-switch/th08.nro touhou08.nro

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: touhou08-nro
          path: touhou08.nro

      # На теге v* дополнительно считаем чексумму — она кладётся в релиз рядом с NRO.
      - name: Checksum
        run: sha256sum touhou08.nro > touhou08.nro.sha256

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            touhou08.nro
            touhou08.nro.sha256
          body_path: RELEASE_NOTES.md
          fail_on_unmatched_files: true
